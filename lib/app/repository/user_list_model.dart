import 'package:flutter/material.dart';
class UserListModel extends StatelessWidget {
  const UserListModel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
